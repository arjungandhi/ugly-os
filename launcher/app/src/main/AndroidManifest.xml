<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      The monkey dir (and its todo.txt) is a real path we read directly and
      watch with FileObserver, kept in sync by Syncthing. That needs broad
      filesystem access; the user grants it once via the all-files screen.
    -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- Global search matches device contacts by name; requested at runtime. -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Home screen shows your next event; requested at runtime. -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <!--
      Lets us enumerate launchable apps without the QUERY_ALL_PACKAGES
      permission. A launcher legitimately needs to see apps that expose a
      MAIN/LAUNCHER activity.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <!-- Launched by package name, so make them explicitly visible. -->
        <package android:name="com.beeper.android" />
        <package android:name="com.futo.platformplayer" />
        <package android:name="com.google.android.apps.walletnfcrel" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.UglyOs">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true"
            android:theme="@style/Theme.UglyOs">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
